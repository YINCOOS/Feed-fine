import React from 'react';
import {Text, View} from 'react-native';
import {AccountBackground, AccountCountainer, AccountCover, AuthButton, AuthInput} from '../components/account.styles';

export const LoginScreen = () => {
    return(
        <AccountBackground/>
      )
};
